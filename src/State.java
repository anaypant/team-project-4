package src;

public enum State {
    IDLE,
    CREATE_USER,
    LOGIN_USER,
    LOGIN_USER_PASSWORD,
    CREATE_POST_CONTENT,
    CREATE_POST_IMAGE,
    SELECT_POST_USERNAME,
    SELECT_POST_CHOICE,
    // Add other states as needed
}
